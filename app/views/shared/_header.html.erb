

<%= link_to 'Reportes de Incidencia', reports_path if Current.user%>

<%= link_to 'Perfil',  user_path(Current.user::id) if Current.user %>
<%= link_to 'Cerrar Sesion',  session_path(Current.user::id), data: {  
    turbo_method: :delete,
    turbo_confirm: '¿Cerrar sesion?'
} if Current.user %>
